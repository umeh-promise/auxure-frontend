const OrderQuantity = ({ className }) => {
  return (
    <span
      className={`flex flex-col justify-center items-center gap-0.625rem rounded-[2.56rem] bg-accent-1 font-bold text-lighter absolute leading-0.5 ${className}`}
    >
      2
    </span>
  );
};

export default OrderQuantity;
